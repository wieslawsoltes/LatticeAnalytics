{
  "name": "lattice-analytics",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local-first visual analytics with a columnar worker engine and instanced WebGPU charts.",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node tools/build.mjs",
    "sample": "node tools/sample.mjs",
    "build:pages": "node tools/build.mjs && node tools/sample.mjs && node tools/pages.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
