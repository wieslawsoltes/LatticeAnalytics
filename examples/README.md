# Sample files

All values and names in these files are synthetic.

Import orders.csv and regions.json, then create a left join on Region. The six input orders contain Sales totaling 540 and Profit totaling 142. Every order has a matching region, so the result contains six rows; the repeated regional target is an attribute, not an additive per-order measure. West contains three orders with Sales totaling 190.

commerce.lattice is a portable 7,200-transaction workbook with four worksheets and the initial linked dashboard. Open it with File → Open workbook. It contains base records and schemas, not precomputed chart marks.
